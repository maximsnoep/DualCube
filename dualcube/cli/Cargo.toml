[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
dualcube = { path = "../core" }
io = {path = "../io" }
mehsh = { path = "../../utils/mehsh", features = ["obj", "stl", "petgraph"] }
grapff = { path = "../../utils/grapff" }

rayon = "1.8.1"
itertools = "0.15.0"
ordered-float = "5.0.0"
log = "0.4.27"
rand = "0.10.0"
enum-iterator = "2.1.0"
anyhow = "1.0"

slotmap = "1.0.7"

[lints]
workspace = true
